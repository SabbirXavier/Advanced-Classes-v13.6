rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Completely open rules as requested by user to resolve blocking issues
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
